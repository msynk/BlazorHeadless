@page "/button"
@rendermode InteractiveServer

<PageTitle>Button — HeadlessUI.Blazor</PageTitle>

<div class="demo-page">
    <header class="demo-header">
        <h1>Button</h1>
        <p>A polymorphic, accessible button primitive with loading and disabled states.</p>
    </header>

    <DemoBlock Title="Basic Button"
               Code="@_basicCode"
               Css="@_btnCss">
        <Description>
            Renders a native <code>&lt;button&gt;</code> with no styling. All visuals come from you.
        </Description>
        <LiveContent>
            <HButton OnClick="HandleBasicClick" class="btn">
                Click me
            </HButton>
            <span class="demo-output">Clicked @basicClickCount times</span>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Disabled State"
               Code="@_disabledCode"
               Css="@_btnDisabledCss">
        <Description>
            Sets the native <code>disabled</code> attribute and emits <code>data-disabled</code> for CSS hooks.
        </Description>
        <LiveContent>
            <HButton Disabled="true" OnClick="HandleBasicClick" class="btn">
                Can't click me
            </HButton>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Loading State with Context"
               Code="@_loadingCode"
               Css="@_btnLoadingCss">
        <Description>
            Uses <code>RenderFragment&lt;ButtonContext&gt;</code> so you can conditionally render
            a spinner driven by the button's own state.
        </Description>
        <LiveContent>
            <HButton Loading="@isLoading" OnClick="HandleLoadingClick" class="btn" Context="btn">
                @if (btn.Loading)
                {
                    <span class="spinner"></span>
                    <span>Saving…</span>
                }
                else
                {
                    <span>Save Changes</span>
                }
            </HButton>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Polymorphic Rendering"
               Code="@_polymorphicCode"
               Css="@_btnLinkCss">
        <Description>
            Set <code>As="a"</code> to render as an anchor while retaining all button semantics.
            Non-native elements automatically receive <code>role="button"</code>, <code>tabindex="0"</code>,
            and Enter/Space keyboard handling.
        </Description>
        <LiveContent>
            <HButton As="a" href="https://github.com" target="_blank" class="btn btn-link">
                I'm actually an &lt;a&gt; tag
            </HButton>
            <HButton As="span" OnClick="HandleBasicClick" class="btn">
                I'm a &lt;span&gt; with role="button"
            </HButton>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Toggle Button"
               Code="@_toggleCode"
               Css="@_btnToggleCss">
        <Description>
            Combine <code>HButton</code> with your own state to build a toggle. Pass
            <code>aria-pressed</code> via standard HTML attributes — they merge automatically.
        </Description>
        <LiveContent>
            <HButton OnClick="TogglePressed"
                     class="@ToggleClass"
                     aria-pressed="@isPressed.ToString().ToLowerInvariant()">
                @(isPressed ? "ON" : "OFF")
            </HButton>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Styled via Data Attributes"
               Code="@_dataAttrCode"
               Css="@_dataAttrCss">
        <Description>
            Style entirely via CSS attribute selectors. No component API for colors, sizes, or
            variants — that's your domain.
        </Description>
        <LiveContent>
            <HButton OnClick="HandleBasicClick" class="btn-data">
                Styled via [data-*]
            </HButton>
            <HButton Disabled="true" class="btn-data">
                Disabled via [data-disabled]
            </HButton>
            <HButton Loading="true" class="btn-data">
                Loading via [data-loading]
            </HButton>
        </LiveContent>
    </DemoBlock>
</div>

@code {
    private int basicClickCount;
    private bool isLoading;
    private bool isPressed;

    private string ToggleClass => isPressed ? "btn btn-toggle active" : "btn btn-toggle";

    private void HandleBasicClick() => basicClickCount++;

    private async Task HandleLoadingClick()
    {
        isLoading = true;
        await Task.Delay(2000);
        isLoading = false;
    }

    private void TogglePressed() => isPressed = !isPressed;

    // ── Source snippets shown alongside each live demo ─────────────────────────

    private const string _basicCode = """
<HButton OnClick="HandleBasicClick" class="btn">
    Click me
</HButton>
""";

    private const string _disabledCode = """
<HButton Disabled="true" class="btn">
    Can't click me
</HButton>
""";

    private const string _loadingCode = """
<HButton Loading="@isLoading" OnClick="Save" class="btn" Context="btn">
    @if (btn.Loading)
    {
        <span class="spinner"></span>
        <span>Saving…</span>
    }
    else
    {
        <span>Save Changes</span>
    }
</HButton>
""";

    private const string _polymorphicCode = """
<HButton As="a" href="https://github.com" target="_blank" class="btn btn-link">
    I'm actually an <a> tag
</HButton>

<HButton As="span" OnClick="HandleClick" class="btn">
    I'm a <span> with role="button"
</HButton>
""";

    private const string _toggleCode = """
<HButton OnClick="Toggle"
         class="@(isPressed ? "btn btn-toggle active" : "btn btn-toggle")"
         aria-pressed="@isPressed.ToString().ToLowerInvariant()">
    @(isPressed ? "ON" : "OFF")
</HButton>
""";

    private const string _dataAttrCode = """
<HButton OnClick="HandleClick" class="btn-data">
    Styled via [data-*]
</HButton>

<HButton Disabled="true" class="btn-data">
    Disabled via [data-disabled]
</HButton>

<HButton Loading="true" class="btn-data">
    Loading via [data-loading]
</HButton>
""";

    private const string _btnCss = """
.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 500;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    background: #fff;
    color: #18181b;
    cursor: pointer;
}

.btn:hover         { background: #f4f4f5; border-color: #d4d4d8; }
.btn:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }
.btn:active        { background: #e4e4e7; }
""";

    private const string _btnDisabledCss = """
/* Hooks emitted by HButton when Disabled="true" */
.btn[data-disabled] {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
}
""";

    private const string _btnLoadingCss = """
/* Hooks emitted by HButton when Loading="true" */
.btn[data-loading] {
    opacity: 0.7;
    cursor: wait;
}

.spinner {
    display: inline-block;
    width: 1rem;
    height: 1rem;
    border: 2px solid currentColor;
    border-right-color: transparent;
    border-radius: 50%;
    animation: spin 0.6s linear infinite;
}

@keyframes spin { to { transform: rotate(360deg); } }
""";

    private const string _btnLinkCss = """
/* Anchor variant of HButton (As="a") */
.btn-link {
    background: transparent;
    border-color: transparent;
    color: #2563eb;
    text-decoration: underline;
    text-underline-offset: 2px;
}

.btn-link:hover { color: #1d4ed8; background: #eff6ff; }
""";

    private const string _btnToggleCss = """
/* Toggle button — driven by your own aria-pressed state */
.btn-toggle {
    min-width: 4rem;
    justify-content: center;
}

.btn-toggle.active {
    background: #2563eb;
    border-color: #2563eb;
    color: white;
}
""";

    private const string _dataAttrCss = """
/* Style entirely via CSS attribute selectors emitted by HButton */
.btn-data                { background: #18181b; color: white; }
.btn-data[data-disabled] { opacity: 0.4; cursor: not-allowed; }
.btn-data[data-loading]  { opacity: 0.7; cursor: wait; }
""";
}
