@*
    Reusable wrapper that renders a section heading, an optional description, and
    a tabbed view with Preview / Razor / CSS panels. Uses the library's own BhTabGroup
    so the demo app dogfoods the headless tab component.
*@

<section class="demo-section">
    @if (!string.IsNullOrWhiteSpace(Title))
    {
        <h2>@Title</h2>
    }
    @if (Description is not null)
    {
        <p>@Description</p>
    }

    <BhTabGroup class="demo-tabs">
        <BhTabList class="demo-tab-list">
            @if (LiveContent is not null)
            {
                <BhTab class="demo-tab">Preview</BhTab>
            }
            @if (!string.IsNullOrWhiteSpace(Code))
            {
                <BhTab class="demo-tab">Razor</BhTab>
            }
            @if (!string.IsNullOrWhiteSpace(Css))
            {
                <BhTab class="demo-tab">CSS</BhTab>
            }
        </BhTabList>
        <BhTabPanels class="demo-tab-panels">
            @if (LiveContent is not null)
            {
                <BhTabPanel class="demo-tab-panel">
                    <div class="demo-row">
                        @LiveContent
                    </div>
                </BhTabPanel>
            }
            @if (!string.IsNullOrWhiteSpace(Code))
            {
                <BhTabPanel class="demo-tab-panel">
                    <pre class="demo-code"><code>@Code.Trim('\r', '\n')</code></pre>
                </BhTabPanel>
            }
            @if (!string.IsNullOrWhiteSpace(Css))
            {
                <BhTabPanel class="demo-tab-panel">
                    <pre class="demo-code"><code>@Css.Trim('\r', '\n')</code></pre>
                </BhTabPanel>
            }
        </BhTabPanels>
    </BhTabGroup>
</section>

@code {
    [Parameter] public string? Title { get; set; }
    [Parameter] public RenderFragment? Description { get; set; }
    [Parameter] public RenderFragment? LiveContent { get; set; }

    /// <summary>The Razor markup used by the demo.</summary>
    [Parameter] public string? Code { get; set; }

    /// <summary>The CSS that styles the demo. Optional.</summary>
    [Parameter] public string? Css { get; set; }
}
