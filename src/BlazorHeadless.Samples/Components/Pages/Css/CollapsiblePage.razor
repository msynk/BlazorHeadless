@page "/collapsible"
@rendermode InteractiveServer

<PageTitle>Collapsible — BlazorHeadless</PageTitle>

<div class="demo-page">
    <header class="demo-header">
        <h1>Collapsible</h1>
        <p>An interactive component which expands and collapses a panel.</p>
        <SectionSwitcher Slug="collapsible" />
    </header>

    <DemoBlock Title="Basic Collapsible"
               Code="@_basicCode"
               Css="@_collapsibleCss">
        <Description>
            A <code>BhCollapsibleTrigger</code> toggles a <code>BhCollapsibleContent</code> panel.
            <code>aria-expanded</code>, <code>aria-controls</code>, and <code>aria-labelledby</code>
            are wired automatically.
        </Description>
        <LiveContent>
            <BhCollapsible class="collapsible">
                <div class="collapsible-header">
                    <span>@@peduarte starred 3 repositories</span>
                    <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
                </div>
                <div class="collapsible-row">@@radix-ui/primitives</div>
                <BhCollapsibleContent class="collapsible-content">
                    <div class="collapsible-row">@@radix-ui/colors</div>
                    <div class="collapsible-row">@@radix-ui/themes</div>
                </BhCollapsibleContent>
            </BhCollapsible>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Default Open"
               Code="@_defaultOpenCode"
               Css="@_collapsibleCss">
        <Description>
            Set <code>DefaultOpen="true"</code> to render the panel open initially (uncontrolled).
        </Description>
        <LiveContent>
            <BhCollapsible DefaultOpen="true" class="collapsible">
                <div class="collapsible-header">
                    <span>Already open</span>
                    <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
                </div>
                <BhCollapsibleContent class="collapsible-content">
                    <div class="collapsible-row">I'm open by default. Click the trigger to close me.</div>
                </BhCollapsibleContent>
            </BhCollapsible>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Render Context (rotating chevron + Close)"
               Code="@_renderContextCode"
               Css="@_chevronCss">
        <Description>
            <code>BhCollapsibleTrigger</code> and <code>BhCollapsibleContent</code> expose a
            <code>BhCollapsibleRenderContext</code> via <code>Context="c"</code>. Use it to drive UI
            from the open state and to call <code>c.Close()</code> from anywhere inside.
        </Description>
        <LiveContent>
            <BhCollapsible class="collapsible">
                <div class="collapsible-header">
                    <span>What's inside?</span>
                    <BhCollapsibleTrigger class="collapsible-trigger" Context="c">
                        <span class="collapsible-chevron @(c.IsOpen ? "open" : "")">▾</span>
                    </BhCollapsibleTrigger>
                </div>
                <BhCollapsibleContent class="collapsible-content" Context="c">
                    <div class="collapsible-row">
                        <p>You can close me from inside the content:</p>
                        <button class="btn" @onclick="c.Close">Close panel</button>
                    </div>
                </BhCollapsibleContent>
            </BhCollapsible>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Disabled"
               Code="@_disabledCode"
               Css="@_disabledCss">
        <Description>
            Set <code>Disabled="true"</code> on the root to block interaction. The trigger receives
            both the native <code>disabled</code> attribute and a <code>data-disabled</code> hook.
        </Description>
        <LiveContent>
            <BhCollapsible Disabled="true" class="collapsible">
                <div class="collapsible-header">
                    <span>Can't toggle me</span>
                    <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
                </div>
                <BhCollapsibleContent class="collapsible-content">
                    <div class="collapsible-row">You'll never see this content.</div>
                </BhCollapsibleContent>
            </BhCollapsible>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Controlled"
               Code="@_controlledCode"
               Css="@_controlledCss">
        <Description>
            Drive the open state externally with <code>Open</code> + <code>OnOpenChange</code>.
            The button below opens or closes the collapsible programmatically.
        </Description>
        <LiveContent>
            <div style="display:flex; flex-direction:column; gap:1rem; width:100%">
                <div class="demo-row">
                    <BhButton OnClick="ToggleControlled" class="btn">
                        Toggle from outside
                    </BhButton>
                    <span class="demo-output">Open: @controlledOpen.ToString().ToLowerInvariant()</span>
                </div>

                <BhCollapsible Open="controlledOpen"
                               OnOpenChange="v => controlledOpen = v"
                               class="collapsible">
                    <div class="collapsible-header">
                        <span>Internal trigger</span>
                        <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
                    </div>
                    <BhCollapsibleContent class="collapsible-content">
                        <div class="collapsible-row">
                            Either trigger updates the same state — fully controlled.
                        </div>
                    </BhCollapsibleContent>
                </BhCollapsible>
            </div>
        </LiveContent>
    </DemoBlock>
</div>

@code {
    private bool controlledOpen;

    private void ToggleControlled() => controlledOpen = !controlledOpen;

    // ── Source snippets shown alongside each live demo ─────────────────────────

    private const string _basicCode = """
<BhCollapsible class="collapsible">
    <div class="collapsible-header">
        <span>@peduarte starred 3 repositories</span>
        <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
    </div>
    <div class="collapsible-row">@radix-ui/primitives</div>
    <BhCollapsibleContent class="collapsible-content">
        <div class="collapsible-row">@radix-ui/colors</div>
        <div class="collapsible-row">@radix-ui/themes</div>
    </BhCollapsibleContent>
</BhCollapsible>
""";

    private const string _defaultOpenCode = """
<BhCollapsible DefaultOpen="true" class="collapsible">
    <div class="collapsible-header">
        <span>Already open</span>
        <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
    </div>
    <BhCollapsibleContent class="collapsible-content">
        <div class="collapsible-row">I'm open by default. Click the trigger to close me.</div>
    </BhCollapsibleContent>
</BhCollapsible>
""";

    private const string _renderContextCode = """
<BhCollapsible class="collapsible">
    <div class="collapsible-header">
        <span>What's inside?</span>
        <BhCollapsibleTrigger class="collapsible-trigger" Context="c">
            <span class="collapsible-chevron @(c.IsOpen ? "open" : "")">▾</span>
        </BhCollapsibleTrigger>
    </div>
    <BhCollapsibleContent class="collapsible-content" Context="c">
        <div class="collapsible-row">
            <p>You can close me from inside the content:</p>
            <button class="btn" @onclick="c.Close">Close panel</button>
        </div>
    </BhCollapsibleContent>
</BhCollapsible>
""";

    private const string _disabledCode = """
<BhCollapsible Disabled="true" class="collapsible">
    <div class="collapsible-header">
        <span>Can't toggle me</span>
        <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
    </div>
    <BhCollapsibleContent class="collapsible-content">
        <div class="collapsible-row">You'll never see this content.</div>
    </BhCollapsibleContent>
</BhCollapsible>
""";

    private const string _controlledCode = """
<div style="display:flex; flex-direction:column; gap:1rem; width:100%">
    <div class="demo-row">
        <BhButton OnClick="ToggleControlled" class="btn">
            Toggle from outside
        </BhButton>
        <span class="demo-output">Open: @controlledOpen.ToString().ToLowerInvariant()</span>
    </div>

    <BhCollapsible Open="controlledOpen"
                   OnOpenChange="v => controlledOpen = v"
                   class="collapsible">
        <div class="collapsible-header">
            <span>Internal trigger</span>
            <BhCollapsibleTrigger class="collapsible-trigger">▾</BhCollapsibleTrigger>
        </div>
        <BhCollapsibleContent class="collapsible-content">
            <div class="collapsible-row">
                Either trigger updates the same state — fully controlled.
            </div>
        </BhCollapsibleContent>
    </BhCollapsible>
</div>

@code {
    private bool controlledOpen;

    private void ToggleControlled() => controlledOpen = !controlledOpen;
}
""";

    private const string _collapsibleCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.collapsible {
    width: 100%;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    overflow: hidden;
    background: #fff;
}

.collapsible-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.875rem 1.125rem;
    font-weight: 500;
}

.collapsible-trigger {
    all: unset;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 0.375rem;
    cursor: pointer;
    color: #71717a;
}

.collapsible-trigger:hover         { background: #f4f4f5; }
.collapsible-trigger:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }
.collapsible-trigger[data-disabled]{ opacity: 0.5; cursor: not-allowed; pointer-events: none; }

.collapsible-row {
    padding: 0.75rem 1.125rem;
    color: #71717a;
    border-top: 1px solid #e4e4e7;
}
""";

    private const string _chevronCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.collapsible {
    width: 100%;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    overflow: hidden;
    background: #fff;
}

.collapsible-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.875rem 1.125rem;
    font-weight: 500;
}

.collapsible-trigger {
    all: unset;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 0.375rem;
    cursor: pointer;
    color: #71717a;
}

.collapsible-trigger:hover         { background: #f4f4f5; }
.collapsible-trigger:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

.collapsible-row {
    padding: 0.75rem 1.125rem;
    color: #71717a;
    border-top: 1px solid #e4e4e7;
}

/* Rotating chevron driven by BhCollapsibleRenderContext.IsOpen */
.collapsible-chevron      { display: inline-block; transition: transform 200ms ease; }
.collapsible-chevron.open { transform: rotate(180deg); }

/* "Close panel" button rendered inside the content */
.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    margin-top: 0.5rem;
    padding: 0.45rem 0.85rem;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    background: #fff;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
}
.btn:hover { background: #f4f4f5; }
""";

    private const string _disabledCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.collapsible {
    width: 100%;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    overflow: hidden;
    background: #fff;
}

.collapsible-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.875rem 1.125rem;
    font-weight: 500;
}

.collapsible-trigger {
    all: unset;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 0.375rem;
    cursor: pointer;
    color: #71717a;
}

.collapsible-trigger:hover          { background: #f4f4f5; }
.collapsible-trigger:focus-visible  { outline: 2px solid #2563eb; outline-offset: 2px; }
.collapsible-trigger[data-disabled] { opacity: 0.5; cursor: not-allowed; pointer-events: none; }

.collapsible-row {
    padding: 0.75rem 1.125rem;
    color: #71717a;
    border-top: 1px solid #e4e4e7;
}

/* The root <BhCollapsible> element also receives data-disabled — useful for
   dimming the entire region. */
.collapsible[data-disabled] {
    background: #fafafa;
}
""";

    private const string _controlledCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.collapsible {
    width: 100%;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    overflow: hidden;
    background: #fff;
}

.collapsible-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.875rem 1.125rem;
    font-weight: 500;
}

.collapsible-trigger {
    all: unset;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 0.375rem;
    cursor: pointer;
    color: #71717a;
}

.collapsible-trigger:hover         { background: #f4f4f5; }
.collapsible-trigger:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

.collapsible-row {
    padding: 0.75rem 1.125rem;
    color: #71717a;
    border-top: 1px solid #e4e4e7;
}

/* External trigger row + state readout */
.demo-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
}

.demo-output {
    font-size: 0.875rem;
    color: #71717a;
}

.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.45rem 0.85rem;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    background: #fff;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
}
.btn:hover { background: #f4f4f5; }
""";
}
