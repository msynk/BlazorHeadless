@page "/radiogroup"
@rendermode InteractiveServer

<PageTitle>Radio Group — BlazorHeadless</PageTitle>

<div class="demo-page">
    <header class="demo-header">
        <h1>Radio Group</h1>
        <p>An accessible single-select group with roving tabindex and full keyboard support.</p>
        <SectionSwitcher Slug="radiogroup" />
    </header>

    <DemoBlock Title="Basic Radio Group"
               Code="@_basicCode"
               Css="@_radioCss">
        <Description>
            Click a radio or use Up / Down arrow keys (Left / Right when horizontal) to navigate
            and select. Disabled radios are skipped. <code>aria-checked</code>,
            <code>role="radiogroup"</code>, and <code>role="radio"</code> are wired automatically.
        </Description>
        <LiveContent>
            <BhRadioGroup DefaultValue="medium" class="radio-group">
                <label class="radio-label">
                    <BhRadio Value="small" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Small</span>
                </label>
                <label class="radio-label">
                    <BhRadio Value="medium" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Medium</span>
                </label>
                <label class="radio-label">
                    <BhRadio Value="large" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Large</span>
                </label>
            </BhRadioGroup>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Horizontal Orientation"
               Code="@_horizontalCode"
               Css="@_horizontalCss">
        <Description>
            Set <code>Vertical="false"</code> to lay radios out horizontally. Arrow-key
            navigation switches to Left / Right and the group emits
            <code>data-orientation="horizontal"</code> for CSS hooks.
        </Description>
        <LiveContent>
            <BhRadioGroup Vertical="false" DefaultValue="card" class="radio-group">
                <label class="radio-label">
                    <BhRadio Value="card" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Card</span>
                </label>
                <label class="radio-label">
                    <BhRadio Value="bank" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Bank</span>
                </label>
                <label class="radio-label">
                    <BhRadio Value="paypal" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>PayPal</span>
                </label>
            </BhRadioGroup>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Disabled Radio"
               Code="@_disabledCode"
               Css="@_disabledCss">
        <Description>
            Set <code>Disabled="true"</code> on individual radios. Arrow-key navigation
            skips disabled options. Set <code>Disabled="true"</code> on the root group to
            disable everything at once.
        </Description>
        <LiveContent>
            <BhRadioGroup DefaultValue="standard" class="radio-group">
                <label class="radio-label">
                    <BhRadio Value="standard" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Standard shipping (3–5 days)</span>
                </label>
                <label class="radio-label">
                    <BhRadio Value="express" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Express shipping (1–2 days)</span>
                </label>
                <label class="radio-label">
                    <BhRadio Value="same-day" Disabled="true" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <span>Same-day shipping (unavailable in your region)</span>
                </label>
            </BhRadioGroup>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Card-style options"
               Code="@_cardCode"
               Css="@_cardCss">
        <Description>
            Because the radio is the only interactive element, you can place rich content
            (titles, descriptions, prices) inside the <code>&lt;label&gt;</code> wrapper
            and use the <code>data-state="checked"</code> hook to highlight the selected card.
        </Description>
        <LiveContent>
            <BhRadioGroup DefaultValue="pro" class="plan-group">
                <label class="plan-card">
                    <BhRadio Value="free" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <div class="plan-info">
                        <strong>Free</strong>
                        <span>Up to 3 projects.</span>
                    </div>
                </label>
                <label class="plan-card">
                    <BhRadio Value="pro" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <div class="plan-info">
                        <strong>Pro — $9 / mo</strong>
                        <span>Unlimited projects, priority support.</span>
                    </div>
                </label>
                <label class="plan-card">
                    <BhRadio Value="team" class="radio" Context="r">
                        @if (r.IsChecked) { <span class="radio-dot"></span> }
                    </BhRadio>
                    <div class="plan-info">
                        <strong>Team — $29 / mo</strong>
                        <span>For groups, with shared workspaces.</span>
                    </div>
                </label>
            </BhRadioGroup>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Controlled"
               Code="@_controlledCode"
               Css="@_controlledCss">
        <Description>
            Drive selection externally with <code>Value</code> and <code>OnValueChange</code>.
        </Description>
        <LiveContent>
            <div style="display:flex; flex-direction:column; gap:1rem; width:100%">
                <div class="demo-row">
                    <BhButton OnClick='() => selectedColor = "red"' class="btn">Red</BhButton>
                    <BhButton OnClick='() => selectedColor = "green"' class="btn">Green</BhButton>
                    <BhButton OnClick='() => selectedColor = "blue"' class="btn">Blue</BhButton>
                    <span class="demo-output">Selected: <strong>@(selectedColor ?? "(none)")</strong></span>
                </div>

                <BhRadioGroup Value="@selectedColor"
                             OnValueChange="v => selectedColor = v"
                             class="radio-group">
                    <label class="radio-label">
                        <BhRadio Value="red" class="radio" Context="r">
                            @if (r.IsChecked) { <span class="radio-dot"></span> }
                        </BhRadio>
                        <span>Red</span>
                    </label>
                    <label class="radio-label">
                        <BhRadio Value="green" class="radio" Context="r">
                            @if (r.IsChecked) { <span class="radio-dot"></span> }
                        </BhRadio>
                        <span>Green</span>
                    </label>
                    <label class="radio-label">
                        <BhRadio Value="blue" class="radio" Context="r">
                            @if (r.IsChecked) { <span class="radio-dot"></span> }
                        </BhRadio>
                        <span>Blue</span>
                    </label>
                </BhRadioGroup>
            </div>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Form Submission"
               Code="@_formCode"
               Css="@_formCss">
        <Description>
            Set <code>Name</code> on the group to render a hidden <code>&lt;input&gt;</code>
            carrying the selected value, so plain HTML form posts include the group's selection.
        </Description>
        <LiveContent>
            <form @onsubmit="HandleFormSubmit"
                  @onsubmit:preventDefault="true"
                  style="display:flex; flex-direction:column; gap:0.75rem; align-items:flex-start;">
                <BhRadioGroup Name="size" DefaultValue="m" class="radio-group">
                    <label class="radio-label">
                        <BhRadio Value="s" class="radio" Context="r">
                            @if (r.IsChecked) { <span class="radio-dot"></span> }
                        </BhRadio>
                        <span>Small</span>
                    </label>
                    <label class="radio-label">
                        <BhRadio Value="m" class="radio" Context="r">
                            @if (r.IsChecked) { <span class="radio-dot"></span> }
                        </BhRadio>
                        <span>Medium</span>
                    </label>
                    <label class="radio-label">
                        <BhRadio Value="l" class="radio" Context="r">
                            @if (r.IsChecked) { <span class="radio-dot"></span> }
                        </BhRadio>
                        <span>Large</span>
                    </label>
                </BhRadioGroup>

                <button type="submit" class="btn btn-primary">Submit</button>

                @if (lastSubmitted is not null)
                {
                    <span class="demo-output">@lastSubmitted</span>
                }
            </form>
        </LiveContent>
    </DemoBlock>
</div>

@code {
    private string? selectedColor = "green";
    private string? lastSubmitted;

    private void HandleFormSubmit()
    {
        lastSubmitted = "(submitted — the form posts size=<selected value>)";
    }

    // ── Source snippets shown alongside each live demo ─────────────────────────

    private const string _basicCode = """
<BhRadioGroup DefaultValue="medium" class="radio-group">
    <label class="radio-label">
        <BhRadio Value="small" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Small</span>
    </label>
    <label class="radio-label">
        <BhRadio Value="medium" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Medium</span>
    </label>
    <label class="radio-label">
        <BhRadio Value="large" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Large</span>
    </label>
</BhRadioGroup>
""";

    private const string _horizontalCode = """
<BhRadioGroup Vertical="false" DefaultValue="card" class="radio-group">
    <label class="radio-label">
        <BhRadio Value="card" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Card</span>
    </label>
    <label class="radio-label">
        <BhRadio Value="bank" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Bank</span>
    </label>
    <label class="radio-label">
        <BhRadio Value="paypal" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>PayPal</span>
    </label>
</BhRadioGroup>
""";

    private const string _disabledCode = """
<BhRadioGroup DefaultValue="standard" class="radio-group">
    <label class="radio-label">
        <BhRadio Value="standard" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Standard shipping (3–5 days)</span>
    </label>
    <label class="radio-label">
        <BhRadio Value="express" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Express shipping (1–2 days)</span>
    </label>
    <label class="radio-label">
        <BhRadio Value="same-day" Disabled="true" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <span>Same-day shipping (unavailable in your region)</span>
    </label>
</BhRadioGroup>
""";

    private const string _cardCode = """
<BhRadioGroup DefaultValue="pro" class="plan-group">
    <label class="plan-card">
        <BhRadio Value="free" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <div class="plan-info">
            <strong>Free</strong>
            <span>Up to 3 projects.</span>
        </div>
    </label>
    <label class="plan-card">
        <BhRadio Value="pro" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <div class="plan-info">
            <strong>Pro — $9 / mo</strong>
            <span>Unlimited projects, priority support.</span>
        </div>
    </label>
    <label class="plan-card">
        <BhRadio Value="team" class="radio" Context="r">
            @if (r.IsChecked) { <span class="radio-dot"></span> }
        </BhRadio>
        <div class="plan-info">
            <strong>Team — $29 / mo</strong>
            <span>For groups, with shared workspaces.</span>
        </div>
    </label>
</BhRadioGroup>
""";

    private const string _controlledCode = """
<div style="display:flex; flex-direction:column; gap:1rem; width:100%">
    <div class="demo-row">
        <BhButton OnClick='() => selectedColor = "red"' class="btn">Red</BhButton>
        <BhButton OnClick='() => selectedColor = "green"' class="btn">Green</BhButton>
        <BhButton OnClick='() => selectedColor = "blue"' class="btn">Blue</BhButton>
        <span class="demo-output">Selected: <strong>@(selectedColor ?? "(none)")</strong></span>
    </div>

    <BhRadioGroup Value="@selectedColor"
                 OnValueChange="v => selectedColor = v"
                 class="radio-group">
        <label class="radio-label">
            <BhRadio Value="red" class="radio" Context="r">
                @if (r.IsChecked) { <span class="radio-dot"></span> }
            </BhRadio>
            <span>Red</span>
        </label>
        <label class="radio-label">
            <BhRadio Value="green" class="radio" Context="r">
                @if (r.IsChecked) { <span class="radio-dot"></span> }
            </BhRadio>
            <span>Green</span>
        </label>
        <label class="radio-label">
            <BhRadio Value="blue" class="radio" Context="r">
                @if (r.IsChecked) { <span class="radio-dot"></span> }
            </BhRadio>
            <span>Blue</span>
        </label>
    </BhRadioGroup>
</div>

@code {
    private string? selectedColor = "green";
}
""";

    private const string _formCode = """
<form @onsubmit="HandleFormSubmit"
      @onsubmit:preventDefault="true"
      style="display:flex; flex-direction:column; gap:0.75rem; align-items:flex-start;">
    <BhRadioGroup Name="size" DefaultValue="m" class="radio-group">
        <label class="radio-label">
            <BhRadio Value="s" class="radio" Context="r">
                @if (r.IsChecked) { <span class="radio-dot"></span> }
            </BhRadio>
            <span>Small</span>
        </label>
        <label class="radio-label">
            <BhRadio Value="m" class="radio" Context="r">
                @if (r.IsChecked) { <span class="radio-dot"></span> }
            </BhRadio>
            <span>Medium</span>
        </label>
        <label class="radio-label">
            <BhRadio Value="l" class="radio" Context="r">
                @if (r.IsChecked) { <span class="radio-dot"></span> }
            </BhRadio>
            <span>Large</span>
        </label>
    </BhRadioGroup>

    <button type="submit" class="btn btn-primary">Submit</button>

    @if (lastSubmitted is not null)
    {
        <span class="demo-output">@lastSubmitted</span>
    }
</form>

@code {
    private string? lastSubmitted;

    private void HandleFormSubmit()
    {
        lastSubmitted = "(submitted — the form posts size=<selected value>)";
    }
}

@* The group renders a single hidden input — posts:  size=<selected value> *@
""";

    private const string _radioCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
}

.radio-group[data-orientation="horizontal"] {
    flex-direction: row;
    gap: 1.25rem;
}

.radio-label {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    cursor: pointer;
    user-select: none;
    font-size: 0.9rem;
}

.radio {
    /* Reset native button */
    all: unset;
    box-sizing: border-box;

    /* Outer ring */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.15rem;
    height: 1.15rem;
    border: 1px solid #d4d4d8;
    border-radius: 999px;
    background: #fff;
    cursor: pointer;
    transition: border-color 120ms ease;
    flex-shrink: 0;
}

.radio:hover         { border-color: #a1a1af; }
.radio:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

/* Hooks emitted by BhRadio */
.radio[data-state="checked"] { border-color: #2563eb; }
.radio[data-disabled]        { opacity: 0.5; cursor: not-allowed; }

/* Inner dot */
.radio-dot {
    width: 0.55rem;
    height: 0.55rem;
    background: #2563eb;
    border-radius: 999px;
}
""";

    private const string _horizontalCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
}

/* The group emits data-orientation="horizontal" when Vertical="false" */
.radio-group[data-orientation="horizontal"] {
    flex-direction: row;
    gap: 1.25rem;
}

.radio-label {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    cursor: pointer;
    user-select: none;
    font-size: 0.9rem;
}

.radio {
    /* Reset native button */
    all: unset;
    box-sizing: border-box;

    /* Outer ring */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.15rem;
    height: 1.15rem;
    border: 1px solid #d4d4d8;
    border-radius: 999px;
    background: #fff;
    cursor: pointer;
    transition: border-color 120ms ease;
    flex-shrink: 0;
}

.radio:hover         { border-color: #a1a1af; }
.radio:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

/* Hooks emitted by BhRadio */
.radio[data-state="checked"] { border-color: #2563eb; }
.radio[data-disabled]        { opacity: 0.5; cursor: not-allowed; }

/* Inner dot */
.radio-dot {
    width: 0.55rem;
    height: 0.55rem;
    background: #2563eb;
    border-radius: 999px;
}
""";

    private const string _disabledCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
}

.radio-group[data-orientation="horizontal"] {
    flex-direction: row;
    gap: 1.25rem;
}

.radio-label {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    cursor: pointer;
    user-select: none;
    font-size: 0.9rem;
}

.radio {
    /* Reset native button */
    all: unset;
    box-sizing: border-box;

    /* Outer ring */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.15rem;
    height: 1.15rem;
    border: 1px solid #d4d4d8;
    border-radius: 999px;
    background: #fff;
    cursor: pointer;
    transition: border-color 120ms ease;
    flex-shrink: 0;
}

.radio:hover         { border-color: #a1a1af; }
.radio:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

/* Hooks emitted by BhRadio */
.radio[data-state="checked"] { border-color: #2563eb; }

/* Hooks emitted when Disabled="true" on a radio (or the root group).
   Arrow-key navigation skips disabled radios automatically. */
.radio[data-disabled] {
    opacity: 0.5;
    cursor: not-allowed;
}

/* Inner dot */
.radio-dot {
    width: 0.55rem;
    height: 0.55rem;
    background: #2563eb;
    border-radius: 999px;
}
""";

    private const string _cardCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.radio {
    /* Reset native button */
    all: unset;
    box-sizing: border-box;

    /* Outer ring */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.15rem;
    height: 1.15rem;
    border: 1px solid #d4d4d8;
    border-radius: 999px;
    background: #fff;
    cursor: pointer;
    transition: border-color 120ms ease;
    flex-shrink: 0;
}

.radio:hover         { border-color: #a1a1af; }
.radio:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

/* Hooks emitted by BhRadio */
.radio[data-state="checked"] { border-color: #2563eb; }
.radio[data-disabled]        { opacity: 0.5; cursor: not-allowed; }

/* Inner dot */
.radio-dot {
    width: 0.55rem;
    height: 0.55rem;
    background: #2563eb;
    border-radius: 999px;
}

/* Card-style group: each label is the styled selectable surface,
   driven by the data-state attribute on the inner radio. */
.plan-group {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
}

.plan-card {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    padding: 0.85rem 1rem;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    cursor: pointer;
    transition: border-color 120ms ease, background 120ms ease;
}

.plan-card:has(.radio[data-state="checked"]) {
    border-color: #2563eb;
    background: #eff6ff;
}

.plan-info {
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
}

.plan-info strong { font-size: 0.95rem; color: #18181b; }
.plan-info span   { font-size: 0.8rem; color: #71717a; }
""";

    private const string _controlledCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
}

.radio-group[data-orientation="horizontal"] {
    flex-direction: row;
    gap: 1.25rem;
}

.radio-label {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    cursor: pointer;
    user-select: none;
    font-size: 0.9rem;
}

.radio {
    /* Reset native button */
    all: unset;
    box-sizing: border-box;

    /* Outer ring */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.15rem;
    height: 1.15rem;
    border: 1px solid #d4d4d8;
    border-radius: 999px;
    background: #fff;
    cursor: pointer;
    transition: border-color 120ms ease;
    flex-shrink: 0;
}

.radio:hover         { border-color: #a1a1af; }
.radio:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

/* Hooks emitted by BhRadio */
.radio[data-state="checked"] { border-color: #2563eb; }
.radio[data-disabled]        { opacity: 0.5; cursor: not-allowed; }

/* Inner dot */
.radio-dot {
    width: 0.55rem;
    height: 0.55rem;
    background: #2563eb;
    border-radius: 999px;
}

/* Controlled mode adds no new CSS hooks — data-state simply reflects
   whatever Value you bind to. Below are the external trigger controls. */
.demo-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
}

.demo-output { font-size: 0.875rem; color: #71717a; }

.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.45rem 0.85rem;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    background: #fff;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
}
.btn:hover { background: #f4f4f5; }
""";

    private const string _formCss = """
/* All styles are consumer-authored — the library ships zero CSS */
.radio-group {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
}

.radio-group[data-orientation="horizontal"] {
    flex-direction: row;
    gap: 1.25rem;
}

.radio-label {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    cursor: pointer;
    user-select: none;
    font-size: 0.9rem;
}

.radio {
    /* Reset native button */
    all: unset;
    box-sizing: border-box;

    /* Outer ring */
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.15rem;
    height: 1.15rem;
    border: 1px solid #d4d4d8;
    border-radius: 999px;
    background: #fff;
    cursor: pointer;
    transition: border-color 120ms ease;
    flex-shrink: 0;
}

.radio:hover         { border-color: #a1a1af; }
.radio:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }

/* Hooks emitted by BhRadio */
.radio[data-state="checked"] { border-color: #2563eb; }
.radio[data-disabled]        { opacity: 0.5; cursor: not-allowed; }

/* Inner dot */
.radio-dot {
    width: 0.55rem;
    height: 0.55rem;
    background: #2563eb;
    border-radius: 999px;
}

/* When Name is set BhRadioGroup renders an <input type="hidden"> sibling
   with the current value — no additional CSS is needed for that. */
.demo-output { font-size: 0.875rem; color: #71717a; }

.btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.45rem 0.85rem;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    background: #fff;
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
}
.btn:hover { background: #f4f4f5; }

.btn-primary {
    color: #fff;
    background: #2563eb;
    border-color: #2563eb;
}
.btn-primary:hover {
    background: #1d4ed8;
    border-color: #1d4ed8;
}
""";
}
