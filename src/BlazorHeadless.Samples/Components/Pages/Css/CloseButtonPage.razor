@page "/close-button"
@rendermode InteractiveServer

<PageTitle>CloseButton — BlazorHeadless</PageTitle>

<div class="demo-page">
    <header class="demo-header">
        <h1>CloseButton</h1>
        <p>A utility button pre-wired to close the nearest enclosing Dialog, Popover, or Disclosure — saves boilerplate for dismiss actions.</p>
        <SectionSwitcher Slug="close-button" />
    </header>

    <DemoBlock Title="Inside a Dialog"
               Code="@_dialogCode"
               Css="@_dialogCss">
        <Description>
            Drop <code>&lt;BhCloseButton&gt;</code> anywhere inside a Dialog and it will
            automatically call the dialog's close callback. No need to wire up
            <code>@@onclick</code> or pass state around.
        </Description>
        <LiveContent>
            <BhButton OnClick="() => dialogOpen = true" class="btn btn-primary">
                Open dialog
            </BhButton>

            <BhDialog Open="dialogOpen" OnClose="() => dialogOpen = false">
                <BhDialogBackdrop class="dialog-backdrop" />
                <BhDialogPanel class="dialog-panel">
                    <BhDialogTitle class="dialog-title">Deactivate account</BhDialogTitle>
                    <BhDialogDescription class="dialog-description">
                        This will permanently deactivate your account. All of your data
                        will be permanently removed.
                    </BhDialogDescription>

                    <div class="dialog-actions">
                        <BhCloseButton class="btn">Cancel</BhCloseButton>
                        <BhCloseButton class="btn btn-danger">Deactivate</BhCloseButton>
                    </div>
                </BhDialogPanel>
            </BhDialog>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Inside a Popover"
               Code="@_popoverCode"
               Css="@_popoverCss">
        <Description>
            Use <code>BhCloseButton</code> inside a popover panel to close it on click.
            Combine with the <code>As</code> parameter to render as an anchor or any
            other element.
        </Description>
        <LiveContent>
            <BhPopover class="popover">
                <BhPopoverButton class="popover-button">
                    Solutions ▾
                </BhPopoverButton>
                <BhPopoverPanel class="popover-panel">
                    <h3 class="popover-title">Navigation</h3>
                    <BhCloseButton As="a" href="#analytics" class="popover-nav-link">
                        Analytics
                    </BhCloseButton>
                    <BhCloseButton As="a" href="#engagement" class="popover-nav-link">
                        Engagement
                    </BhCloseButton>
                    <BhCloseButton As="a" href="#security" class="popover-nav-link">
                        Security
                    </BhCloseButton>
                    <hr class="popover-divider" />
                    <BhCloseButton class="popover-dismiss">
                        Dismiss ✕
                    </BhCloseButton>
                </BhPopoverPanel>
            </BhPopover>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Inside a Disclosure"
               Code="@_disclosureCode"
               Css="@_disclosureCss">
        <Description>
            Works the same way inside a Disclosure. The button finds the nearest
            closeable ancestor and calls its close method.
        </Description>
        <LiveContent>
            <BhDisclosure DefaultOpen="true" class="disclosure">
                <BhDisclosureButton class="disclosure-button">
                    Show details
                </BhDisclosureButton>
                <BhDisclosurePanel class="disclosure-panel">
                    <div class="disclosure-body">
                        <p>Here are some details you can collapse.</p>
                        <BhCloseButton class="btn btn-sm">Hide details</BhCloseButton>
                    </div>
                </BhDisclosurePanel>
            </BhDisclosure>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Polymorphic rendering (As)"
               Code="@_polymorphicCode"
               Css="@_polymorphicCss">
        <Description>
            Use the <code>As</code> parameter to render the close button as any HTML element.
            Non-native button elements automatically receive <code>role="button"</code>,
            <code>tabindex="0"</code>, and Enter/Space keyboard handling.
        </Description>
        <LiveContent>
            <BhPopover class="popover">
                <BhPopoverButton class="popover-button">
                    Open panel ▾
                </BhPopoverButton>
                <BhPopoverPanel class="popover-panel">
                    <p class="popover-body">
                        The close actions below render as different elements:
                    </p>
                    <div class="close-variants">
                        <BhCloseButton class="btn btn-sm">
                            Button (default)
                        </BhCloseButton>
                        <BhCloseButton As="a" href="#" class="popover-nav-link">
                            Anchor link
                        </BhCloseButton>
                        <BhCloseButton As="div" class="close-div">
                            Div with role="button"
                        </BhCloseButton>
                    </div>
                </BhPopoverPanel>
            </BhPopover>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Disabled state"
               Code="@_disabledCode"
               Css="@_disabledCss">
        <Description>
            Set <code>Disabled="true"</code> to prevent the close action. The button
            receives the native <code>disabled</code> attribute (or <code>aria-disabled</code>
            for non-native elements) and a <code>data-disabled</code> styling hook.
        </Description>
        <LiveContent>
            <BhPopover class="popover">
                <BhPopoverButton class="popover-button">
                    Panel is open ▾
                </BhPopoverButton>
                <BhPopoverPanel class="popover-panel">
                    <p class="popover-body">
                        The disabled close button below won't close this panel:
                    </p>
                    <div class="close-variants">
                        <BhCloseButton Disabled="true" class="btn btn-sm">
                            Can't close (disabled)
                        </BhCloseButton>
                        <BhCloseButton class="btn btn-sm btn-primary">
                            Close (enabled)
                        </BhCloseButton>
                    </div>
                </BhPopoverPanel>
            </BhPopover>
        </LiveContent>
    </DemoBlock>
</div>

@code {
    private bool dialogOpen;

    // ── Source snippets shown alongside each live demo ─────────────────────────

    private const string _dialogCode = """
<BhButton OnClick="() => isOpen = true" class="btn btn-primary">
    Open dialog
</BhButton>

<BhDialog Open="isOpen" OnClose="() => isOpen = false">
    <BhDialogBackdrop class="dialog-backdrop" />
    <BhDialogPanel class="dialog-panel">
        <BhDialogTitle>Deactivate account</BhDialogTitle>
        <BhDialogDescription>
            This will permanently deactivate your account.
        </BhDialogDescription>

        <div class="dialog-actions">
            <!-- No @onclick needed — BhCloseButton finds the Dialog automatically -->
            <BhCloseButton class="btn">Cancel</BhCloseButton>
            <BhCloseButton class="btn btn-danger">Deactivate</BhCloseButton>
        </div>
    </BhDialogPanel>
</BhDialog>

@code {
    private bool isOpen;
}
""";

    private const string _popoverCode = """
<BhPopover class="popover">
    <BhPopoverButton class="popover-button">Solutions ▾</BhPopoverButton>
    <BhPopoverPanel class="popover-panel">
        <h3 class="popover-title">Navigation</h3>

        <!-- Render as <a> elements that also close the popover on click -->
        <BhCloseButton As="a" href="#analytics" class="popover-nav-link">
            Analytics
        </BhCloseButton>
        <BhCloseButton As="a" href="#engagement" class="popover-nav-link">
            Engagement
        </BhCloseButton>

        <hr class="popover-divider" />
        <BhCloseButton class="popover-dismiss">Dismiss ✕</BhCloseButton>
    </BhPopoverPanel>
</BhPopover>
""";

    private const string _disclosureCode = """
<BhDisclosure DefaultOpen="true" class="disclosure">
    <BhDisclosureButton class="disclosure-button">
        Show details
    </BhDisclosureButton>
    <BhDisclosurePanel class="disclosure-panel">
        <div class="disclosure-body">
            <p>Here are some details you can collapse.</p>
            <BhCloseButton class="btn btn-sm">Hide details</BhCloseButton>
        </div>
    </BhDisclosurePanel>
</BhDisclosure>
""";

    private const string _polymorphicCode = """
<BhPopover class="popover">
    <BhPopoverButton class="popover-button">Open panel ▾</BhPopoverButton>
    <BhPopoverPanel class="popover-panel">
        <!-- Default: renders as <button> -->
        <BhCloseButton class="btn btn-sm">Button (default)</BhCloseButton>

        <!-- Renders as <a> with role="button" and keyboard handling -->
        <BhCloseButton As="a" href="#" class="popover-nav-link">
            Anchor link
        </BhCloseButton>

        <!-- Renders as <div> with role="button", tabindex, keyboard -->
        <BhCloseButton As="div" class="close-div">
            Div with role="button"
        </BhCloseButton>
    </BhPopoverPanel>
</BhPopover>
""";

    private const string _disabledCode = """
<BhPopover class="popover">
    <BhPopoverButton class="popover-button">Panel is open ▾</BhPopoverButton>
    <BhPopoverPanel class="popover-panel">
        <p>The disabled close button won't close this panel:</p>

        <BhCloseButton Disabled="true" class="btn btn-sm">
            Can't close (disabled)
        </BhCloseButton>
        <BhCloseButton class="btn btn-sm btn-primary">
            Close (enabled)
        </BhCloseButton>
    </BhPopoverPanel>
</BhPopover>
""";

    private const string _dialogCss = """
.dialog-backdrop { background: rgba(0, 0, 0, 0.5); }

.dialog-panel {
    margin: 4rem auto;
    max-width: 28rem;
    background: #fff;
    border-radius: 0.75rem;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
}

.dialog-title       { font-size: 1.1rem; font-weight: 600; color: #18181b; }
.dialog-description { font-size: 0.9rem; color: #71717a; }
.dialog-actions     { display: flex; justify-content: flex-end; gap: 0.5rem; margin-top: 0.5rem; }

.btn-danger {
    background: #dc2626;
    border-color: #dc2626;
    color: #fff;
}
""";

    private const string _popoverCss = """
.popover { position: relative; display: inline-block; }

.popover-button {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 0.9rem;
    border: 1px solid #d4d4d8;
    border-radius: 0.5rem;
    background: #fff;
    font-size: 0.9rem;
    cursor: pointer;
}

.popover-panel {
    position: absolute;
    top: calc(100% + 0.5rem);
    left: 0;
    z-index: 31;
    min-width: 14rem;
    background: #fff;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    padding: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
}

.popover-title      { font-size: 0.9rem; font-weight: 600; margin: 0; }
.popover-nav-link   { font-size: 0.85rem; color: #2563eb; text-decoration: none; padding: 0.3rem 0; }
.popover-nav-link:hover { text-decoration: underline; }
.popover-divider    { border: none; border-top: 1px solid #e4e4e7; margin: 0.25rem 0; }

.popover-dismiss {
    all: unset;
    display: block;
    width: 100%;
    font-size: 0.8rem;
    font-family: inherit;
    color: #71717a;
    cursor: pointer;
    text-align: center;
    padding: 0.4rem;
    border-radius: 0.35rem;
    box-sizing: border-box;
}
.popover-dismiss:hover { color: #18181b; background: #f4f4f5; }
.popover-dismiss:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }
""";

    private const string _disclosureCss = """
.disclosure {
    width: 100%;
    border: 1px solid #e4e4e7;
    border-radius: 0.5rem;
    overflow: hidden;
    background: #fff;
}

.disclosure-button {
    all: unset;
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: 0.875rem 1.125rem;
    font-weight: 500;
    cursor: pointer;
    box-sizing: border-box;
}

.disclosure-button:hover              { background: #f4f4f5; }
.disclosure-button[data-state="open"] { background: #f4f4f5; }

.disclosure-body {
    padding: 0.875rem 1.125rem 1rem;
    color: #71717a;
    border-top: 1px solid #e4e4e7;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.btn-sm { font-size: 0.8rem; padding: 0.35rem 0.7rem; }
""";

    private const string _polymorphicCss = """
/* Reuses .popover styles from above */

.close-variants {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-top: 0.5rem;
}

.close-div {
    padding: 0.4rem 0.7rem;
    background: #f4f4f5;
    border-radius: 0.4rem;
    font-size: 0.85rem;
    cursor: pointer;
    text-align: center;
}

.close-div:hover        { background: #e4e4e7; }
.close-div:focus-visible { outline: 2px solid #2563eb; outline-offset: 2px; }
""";

    private const string _disabledCss = """
/* Reuses .popover styles from above */

/* Style the disabled state via the data-disabled attribute */
[data-disabled] {
    opacity: 0.5;
    cursor: not-allowed;
}
""";
}
