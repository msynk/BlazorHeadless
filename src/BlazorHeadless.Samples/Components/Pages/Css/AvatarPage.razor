@page "/avatar"
@rendermode InteractiveServer

<PageTitle>Avatar — BlazorHeadless</PageTitle>

<div class="demo-page">
    <header class="demo-header">
        <h1>Avatar</h1>
        <p>An image element with a graceful fallback for representing a user or entity.</p>
        <SectionSwitcher Slug="avatar" />
    </header>

    <DemoBlock Title="Basic"
               Code="@_basicCode"
               Css="@_basicCss">
        <Description>
            Compose <code>BhAvatarImage</code> with a <code>BhAvatarFallback</code> inside
            <code>BhAvatar</code>. The image is only shown once it loads successfully — until
            then the fallback initials are displayed.
        </Description>
        <LiveContent>
            <BhAvatar class="avatar">
                <BhAvatarImage class="avatar-image"
                               Src="https://images.unsplash.com/photo-1492633423870-43d1cd2775eb?w=128&h=128&dpr=2&q=80"
                               Alt="Colm Tuite" />
                <BhAvatarFallback class="avatar-fallback">CT</BhAvatarFallback>
            </BhAvatar>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Fallback (broken image)"
               Code="@_fallbackCode"
               Css="@_fallbackCss">
        <Description>
            When the image fails to load (or no <code>Src</code> is given), the fallback is
            shown instead of a broken-image icon. Here the source is intentionally invalid.
        </Description>
        <LiveContent>
            <BhAvatar class="avatar">
                <BhAvatarImage class="avatar-image"
                               Src="https://example.com/does-not-exist.jpg"
                               Alt="Jane Doe" />
                <BhAvatarFallback class="avatar-fallback">JD</BhAvatarFallback>
            </BhAvatar>

            <BhAvatar class="avatar">
                <BhAvatarFallback class="avatar-fallback avatar-fallback-icon">
                    <svg viewBox="0 0 24 24" width="22" height="22" fill="none"
                         stroke="currentColor" stroke-width="2" aria-hidden="true">
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                        <circle cx="12" cy="7" r="4" />
                    </svg>
                </BhAvatarFallback>
            </BhAvatar>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Delayed fallback"
               Code="@_delayCode"
               Css="@_delayCss">
        <Description>
            Set <code>DelayMs</code> on the fallback to wait before showing it. This avoids a
            flash of initials for images that load quickly. The fallback only appears if the
            image hasn't loaded by the time the delay elapses.
        </Description>
        <LiveContent>
            <BhAvatar class="avatar">
                <BhAvatarImage class="avatar-image"
                               Src="https://images.unsplash.com/photo-1511485977113-f34c92461ad9?w=128&h=128&dpr=2&q=80"
                               Alt="Pedro Duarte" />
                <BhAvatarFallback class="avatar-fallback" DelayMs="600">PD</BhAvatarFallback>
            </BhAvatar>
        </LiveContent>
    </DemoBlock>

    <DemoBlock Title="Sizes and shapes"
               Code="@_variantsCode"
               Css="@_variantsCss">
        <Description>
            The library ships zero styling, so size and shape are entirely up to your CSS.
            Override <code>class</code> per avatar for circles, squares, or stacked groups.
        </Description>
        <LiveContent>
            <div class="avatar-row">
                <BhAvatar class="avatar avatar-sm">
                    <BhAvatarImage class="avatar-image"
                                   Src="https://images.unsplash.com/photo-1607746882042-944635dfe10e?w=96&h=96&dpr=2&q=80"
                                   Alt="" />
                    <BhAvatarFallback class="avatar-fallback">AB</BhAvatarFallback>
                </BhAvatar>

                <BhAvatar class="avatar">
                    <BhAvatarImage class="avatar-image"
                                   Src="https://images.unsplash.com/photo-1527980965255-d3b416303d12?w=128&h=128&dpr=2&q=80"
                                   Alt="" />
                    <BhAvatarFallback class="avatar-fallback">CD</BhAvatarFallback>
                </BhAvatar>

                <BhAvatar class="avatar avatar-lg avatar-square">
                    <BhAvatarImage class="avatar-image"
                                   Src="https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?w=160&h=160&dpr=2&q=80"
                                   Alt="" />
                    <BhAvatarFallback class="avatar-fallback">EF</BhAvatarFallback>
                </BhAvatar>
            </div>
        </LiveContent>
    </DemoBlock>
</div>

@code {
    private const string _basicCode = """
<BhAvatar class="avatar">
    <BhAvatarImage class="avatar-image" Src="/colm.jpg" Alt="Colm Tuite" />
    <BhAvatarFallback class="avatar-fallback">CT</BhAvatarFallback>
</BhAvatar>
""";

    private const string _fallbackCode = """
@* Broken / missing image → the fallback shows automatically *@
<BhAvatar class="avatar">
    <BhAvatarImage class="avatar-image" Src="/does-not-exist.jpg" Alt="Jane Doe" />
    <BhAvatarFallback class="avatar-fallback">JD</BhAvatarFallback>
</BhAvatar>

@* Fallback with no image at all — e.g. a generic user icon *@
<BhAvatar class="avatar">
    <BhAvatarFallback class="avatar-fallback avatar-fallback-icon">
        <svg viewBox="0 0 24 24" width="22" height="22" ...>...</svg>
    </BhAvatarFallback>
</BhAvatar>
""";

    private const string _delayCode = """
<BhAvatar class="avatar">
    <BhAvatarImage class="avatar-image" Src="/pedro.jpg" Alt="Pedro Duarte" />
    <BhAvatarFallback class="avatar-fallback" DelayMs="600">PD</BhAvatarFallback>
</BhAvatar>
""";

    private const string _variantsCode = """
<div class="avatar-row">
    <BhAvatar class="avatar avatar-sm">
        <BhAvatarImage class="avatar-image" Src="/a.jpg" Alt="" />
        <BhAvatarFallback class="avatar-fallback">AB</BhAvatarFallback>
    </BhAvatar>

    <BhAvatar class="avatar">
        <BhAvatarImage class="avatar-image" Src="/b.jpg" Alt="" />
        <BhAvatarFallback class="avatar-fallback">CD</BhAvatarFallback>
    </BhAvatar>

    <BhAvatar class="avatar avatar-lg avatar-square">
        <BhAvatarImage class="avatar-image" Src="/c.jpg" Alt="" />
        <BhAvatarFallback class="avatar-fallback">EF</BhAvatarFallback>
    </BhAvatar>
</div>
""";

    private const string _basicCss = """
/* The library ships zero CSS — these styles are consumer-authored. */
.avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    vertical-align: middle;
    overflow: hidden;
    user-select: none;
    width: 3rem;
    height: 3rem;
    border-radius: 100%;
    background: #e4e4e7;
}

.avatar-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: inherit;
}

.avatar-fallback {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 100%;
    font-size: 0.95rem;
    font-weight: 600;
    color: #3f3f46;
    background: #e4e4e7;
}
""";

    private const string _fallbackCss = """
/* Reuses .avatar / .avatar-image / .avatar-fallback from the Basic demo. */
.avatar-fallback-icon {
    color: #71717a;
    background: #f4f4f5;
}
""";

    private const string _delayCss = """
/* Reuses the styles from the Basic demo. DelayMs only affects timing,
   not appearance — there are no extra CSS hooks needed. */
""";

    private const string _variantsCss = """
/* Reuses the base styles; these modifiers tweak size and shape. */
.avatar-row {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.avatar-sm { width: 2.25rem; height: 2.25rem; font-size: 0.8rem; }
.avatar-lg { width: 4rem;    height: 4rem;    font-size: 1.1rem; }

.avatar-square { border-radius: 0.6rem; }
""";
}
