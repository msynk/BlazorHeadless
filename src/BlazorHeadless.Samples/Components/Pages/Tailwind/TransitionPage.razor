@page "/tw/transition"
@rendermode InteractiveServer

<PageTitle>Transition — BlazorHeadless · Tailwind</PageTitle>

<div class="mx-auto w-full max-w-3xl px-6 py-12 pb-24">
    <header class="mb-12">
        <h1 class="text-3xl font-bold tracking-tight text-zinc-900">Transition</h1>
        <p class="mt-2 text-zinc-500">CSS class-based enter/leave animations — Tailwind utilities go directly into the Enter/Leave parameters.</p>
        <TwSectionSwitcher Slug="transition" />
    </header>

    <TwDemoBlock Title="Fade" Code="@_fadeCode">
        <Description>Each <code class="rounded bg-zinc-100 px-1.5 py-0.5 text-sm">Enter*</code> / <code class="rounded bg-zinc-100 px-1.5 py-0.5 text-sm">Leave*</code> parameter is a string of utility classes.</Description>
        <LiveContent>
            <div class="flex flex-col gap-4 items-start">
                <BhButton OnClick="() => showFade = !showFade"
                          class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
                    @(showFade ? "Hide" : "Show")
                </BhButton>
                <BhTransition Show="showFade"
                              Enter="transition-opacity duration-300"
                              EnterFrom="opacity-0"
                              EnterTo="opacity-100"
                              Leave="transition-opacity duration-200"
                              LeaveFrom="opacity-100"
                              LeaveTo="opacity-0"
                              class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
                    <p>I fade in and out.</p>
                </BhTransition>
            </div>
        </LiveContent>
    </TwDemoBlock>

    <TwDemoBlock Title="Scale + Fade" Code="@_scaleCode">
        <Description>Combine opacity and transform.</Description>
        <LiveContent>
            <div class="flex flex-col gap-4 items-start">
                <BhButton OnClick="() => showScale = !showScale"
                          class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
                    @(showScale ? "Hide" : "Show")
                </BhButton>
                <BhTransition Show="showScale"
                              Enter="transition-all duration-300 ease-out"
                              EnterFrom="opacity-0 scale-95"
                              EnterTo="opacity-100 scale-100"
                              Leave="transition-all duration-200 ease-in"
                              LeaveFrom="opacity-100 scale-100"
                              LeaveTo="opacity-0 scale-95"
                              class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
                    <p>I scale and fade.</p>
                </BhTransition>
            </div>
        </LiveContent>
    </TwDemoBlock>

    <TwDemoBlock Title="Slide Down" Code="@_slideCode">
        <Description>Use Tailwind's arbitrary value syntax for <code class="rounded bg-zinc-100 px-1.5 py-0.5 text-sm">max-h-[…]</code>.</Description>
        <LiveContent>
            <div class="flex flex-col gap-4 items-start">
                <BhButton OnClick="() => showSlide = !showSlide"
                          class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
                    @(showSlide ? "Hide" : "Show")
                </BhButton>
                <BhTransition Show="showSlide"
                              Enter="transition-all duration-300 ease-out"
                              EnterFrom="max-h-0 opacity-0"
                              EnterTo="max-h-40 opacity-100"
                              Leave="transition-all duration-200 ease-in"
                              LeaveFrom="max-h-40 opacity-100"
                              LeaveTo="max-h-0 opacity-0"
                              class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block overflow-hidden">
                    <p>I slide down from zero height.</p>
                </BhTransition>
            </div>
        </LiveContent>
    </TwDemoBlock>

    <TwDemoBlock Title="With Appear" Code="@_appearCode">
        <Description><code class="rounded bg-zinc-100 px-1.5 py-0.5 text-sm">Appear="true"</code> runs the enter transition on first mount.</Description>
        <LiveContent>
            <div class="flex flex-col gap-4 items-start">
                <BhButton OnClick="() => showAppear = !showAppear"
                          class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
                    @(showAppear ? "Hide" : "Show")
                </BhButton>
                <BhTransition Show="showAppear" Appear="true"
                              Enter="transition-all duration-500 ease-out"
                              EnterFrom="opacity-0 translate-y-4"
                              EnterTo="opacity-100 translate-y-0"
                              Leave="transition-all duration-300 ease-in"
                              LeaveFrom="opacity-100 translate-y-0"
                              LeaveTo="opacity-0 translate-y-4"
                              class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
                    <p>I animate on first mount too.</p>
                </BhTransition>
            </div>
        </LiveContent>
    </TwDemoBlock>

    <TwDemoBlock Title="Lifecycle Callbacks" Code="@_callbackCode">
        <Description>Logic for each phase of the transition.</Description>
        <LiveContent>
            <div class="flex flex-col gap-4 items-start">
                <BhButton OnClick="() => showCallbacks = !showCallbacks"
                          class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
                    @(showCallbacks ? "Hide" : "Show")
                </BhButton>
                <BhTransition Show="showCallbacks"
                              Enter="transition-opacity duration-300" EnterFrom="opacity-0" EnterTo="opacity-100"
                              Leave="transition-opacity duration-200" LeaveFrom="opacity-100" LeaveTo="opacity-0"
                              BeforeEnter='@(() => Log("BeforeEnter"))'
                              AfterEnter='@(() => Log("AfterEnter"))'
                              BeforeLeave='@(() => Log("BeforeLeave"))'
                              AfterLeave='@(() => Log("AfterLeave"))'
                              class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
                    <p>Check the log below.</p>
                </BhTransition>
                <pre class="text-xs text-zinc-500 whitespace-pre-wrap">@callbackLog</pre>
            </div>
        </LiveContent>
    </TwDemoBlock>
</div>

@code {
    private bool showFade;
    private bool showScale;
    private bool showSlide;
    private bool showAppear = true;
    private bool showCallbacks;
    private string callbackLog = "";

    private void Log(string msg) => callbackLog += msg + "\n";

    private const string _fadeCode = """
<div class="flex flex-col gap-4 items-start">
    <BhButton OnClick="() => showFade = !showFade"
              class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
        @(showFade ? "Hide" : "Show")
    </BhButton>
    <BhTransition Show="showFade"
                  Enter="transition-opacity duration-300"
                  EnterFrom="opacity-0"
                  EnterTo="opacity-100"
                  Leave="transition-opacity duration-200"
                  LeaveFrom="opacity-100"
                  LeaveTo="opacity-0"
                  class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
        <p>I fade in and out.</p>
    </BhTransition>
</div>

@code {
    private bool showFade;
}
""";

    private const string _scaleCode = """
<div class="flex flex-col gap-4 items-start">
    <BhButton OnClick="() => showScale = !showScale"
              class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
        @(showScale ? "Hide" : "Show")
    </BhButton>
    <BhTransition Show="showScale"
                  Enter="transition-all duration-300 ease-out"
                  EnterFrom="opacity-0 scale-95"
                  EnterTo="opacity-100 scale-100"
                  Leave="transition-all duration-200 ease-in"
                  LeaveFrom="opacity-100 scale-100"
                  LeaveTo="opacity-0 scale-95"
                  class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
        <p>I scale and fade.</p>
    </BhTransition>
</div>

@code {
    private bool showScale;
}
""";

    private const string _slideCode = """
<div class="flex flex-col gap-4 items-start">
    <BhButton OnClick="() => showSlide = !showSlide"
              class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
        @(showSlide ? "Hide" : "Show")
    </BhButton>
    <BhTransition Show="showSlide"
                  Enter="transition-all duration-300 ease-out"
                  EnterFrom="max-h-0 opacity-0"
                  EnterTo="max-h-40 opacity-100"
                  Leave="transition-all duration-200 ease-in"
                  LeaveFrom="max-h-40 opacity-100"
                  LeaveTo="max-h-0 opacity-0"
                  class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block overflow-hidden">
        <p>I slide down from zero height.</p>
    </BhTransition>
</div>

@code {
    private bool showSlide;
}
""";

    private const string _appearCode = """
<div class="flex flex-col gap-4 items-start">
    <BhButton OnClick="() => showAppear = !showAppear"
              class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
        @(showAppear ? "Hide" : "Show")
    </BhButton>
    <BhTransition Show="showAppear" Appear="true"
                  Enter="transition-all duration-500 ease-out"
                  EnterFrom="opacity-0 translate-y-4"
                  EnterTo="opacity-100 translate-y-0"
                  Leave="transition-all duration-300 ease-in"
                  LeaveFrom="opacity-100 translate-y-0"
                  LeaveTo="opacity-0 translate-y-4"
                  class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
        <p>I animate on first mount too.</p>
    </BhTransition>
</div>

@code {
    private bool showAppear = true;
}
""";

    private const string _callbackCode = """
<div class="flex flex-col gap-4 items-start">
    <BhButton OnClick="() => showCallbacks = !showCallbacks"
              class="inline-flex items-center gap-2 rounded-lg border border-blue-600 bg-blue-600 px-4 py-2 text-sm font-medium text-white cursor-pointer hover:bg-blue-700">
        @(showCallbacks ? "Hide" : "Show")
    </BhButton>
    <BhTransition Show="showCallbacks"
                  Enter="transition-opacity duration-300" EnterFrom="opacity-0" EnterTo="opacity-100"
                  Leave="transition-opacity duration-200" LeaveFrom="opacity-100" LeaveTo="opacity-0"
                  BeforeEnter='@(() => Log("BeforeEnter"))'
                  AfterEnter='@(() => Log("AfterEnter"))'
                  BeforeLeave='@(() => Log("BeforeLeave"))'
                  AfterLeave='@(() => Log("AfterLeave"))'
                  class="rounded-lg border border-zinc-200 bg-white p-4 text-sm text-zinc-900 inline-block">
        <p>Check the log below.</p>
    </BhTransition>
    <pre class="text-xs text-zinc-500 whitespace-pre-wrap">@callbackLog</pre>
</div>

@code {
    private bool showCallbacks;
    private string callbackLog = "";

    private void Log(string msg) => callbackLog += msg + "\n";
}
""";
}
